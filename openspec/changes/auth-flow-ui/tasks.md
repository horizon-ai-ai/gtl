## 1. Resend-verification endpoint

- [x] 1.1 Deliver the "Resend-verification endpoint re-issues a token for an unverified user" requirement: add a `POST` handler at `src/app/api/auth/resend-verification/route.ts` that resolves the target email from the session first and the request body `email` second — the design decision "Resend identifies the user by session, falling back to an email field" — returning `400 VALIDATION_ERROR` when neither is present, and for an `active` user with `email_verified_at` null calls `issueToken(user_id, "verify")` then `sendVerifyEmail`, otherwise no-ops; per "Resend mirrors forgot-password anti-enumeration" it returns `200 { data: {} }` whenever an email resolved. Verified by the resolution matrix behavior and the test in task 1.2.
- [x] 1.2 Add `src/app/api/auth/resend-verification/route.test.ts` covering: active unverified user → `issueToken` + `sendVerifyEmail` called once; session present with body email omitted → resolves from session and sends; unknown / non-active / already-verified email → no token, no send, still `200`; missing email and no session → `VALIDATION_ERROR`. Verified by `npm test -- resend-verification` passing.

## 2. Email link paths

- [x] 2.1 Deliver the "Verification email links to the verify page" and "Password-reset email links to the reset page" requirements by updating `src/lib/auth/emails.ts` so `sendVerifyEmail` links to the `/verify` route and `sendPasswordResetEmail` links to the `/reset` route, each carrying the plaintext token as a `token` query parameter — the design decision "Email link paths updated to the group-relative routes". Verified by updating `src/lib/auth/emails.test.ts` to assert the new `/verify?token=` and `/reset?token=` link targets and the suite passing.

## 3. Recovery pages

- [x] 3.1 Deliver the "Forgot-password page requests a reset link" requirement: add `src/app/(auth)/forgot/page.tsx` rendering inside the existing group per the design decision "Pages live inside the existing `(auth)` route group", with an email field that posts to `POST /api/auth/forgot-password` and shows one neutral confirmation regardless of outcome plus a link to `/login`. Verified manually against the forgot-page scenarios (neutral confirmation shown, no account-existence leak).
- [x] 3.2 Deliver the "Reset-password page rotates the password from an emailed link" requirement: add `src/app/(auth)/reset/page.tsx` that reads the `token` query param, renders an invalid-link state with a `/forgot` link when absent, otherwise shows new-password + confirm fields, blocks submit unless the password is ≥ 8 chars and the two match, posts `{ token, new_password }` to `POST /api/auth/reset-password`, redirects to `/login` on `200`, and shows a failure message with a `/forgot` link on error. Verified manually against the reset-page scenarios (success redirect, missing-token state, blocked submit, rejected-token recovery path).
- [x] 3.3 Deliver the "Login page links to forgot-password" requirement: add a "忘記密碼？" link to `/forgot` on `src/app/(auth)/login/page.tsx`. Verified manually that the login page shows the link and it navigates to `/forgot`.

## 4. Verify-email page

- [x] 4.1 Deliver the "Verify-email landing page renders the verification result" requirement: add `src/app/(auth)/verify/page.tsx` that reads the `token` query param and calls `GET /api/auth/verify-email` exactly once on mount using a `useRef` ran-once guard — the design decision "Verify page calls verify-email once via a ran-once guard" — rendering loading / verified / already-verified / dead-link states, linking verified states to `/login`, and exposing a resend control (email prefilled from session when signed in) that posts to `POST /api/auth/resend-verification` with a neutral confirmation. Verified manually against the verify-page scenarios (single endpoint call, each state rendered, resend confirmation neutral).
