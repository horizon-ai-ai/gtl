## 1. Dependencies and font asset

- [x] 1.1 Add `@react-pdf/renderer` as a production dependency per the "Use @react-pdf/renderer for rendering" decision. Verify by running `npm install` and asserting the package appears under `dependencies` in `package.json` and resolves via `import { renderToBuffer, Document, Page, View, Text, StyleSheet, Font } from '@react-pdf/renderer'` in a TypeScript file.
- [x] 1.2 Vendor `NotoSansCJK-Regular.ttf` into `src/lib/pdf/fonts/` per the "Bundle Noto Sans CJK font in the repo" decision so the bundled font is shipped alongside the route module. Verify by `ls -lh src/lib/pdf/fonts/NotoSansCJK-Regular.ttf` reporting a file between 5 MB and 17 MB and the path appearing in `git status` as a tracked binary asset. (Note: the legacy Adobe Noto Sans CJK TC subset OTF — saved with a `.ttf` extension since fontkit reads OTF by magic bytes — is the only Noto distribution whose name table contains the literal substring `Noto Sans CJK` that task 3.2 asserts, and it ships at ~15.7 MiB.)

## 2. PIData interface and adapter (Adapter purity, Em-dash fallback)

- [x] 2.1 Define the `PIData` TypeScript interface in `src/lib/pdf/pi-data.ts` covering the section shapes named in Implementation Contract (invoice metadata, seller block, customer block, shipping details, line items array, totals breakdown, additional details, signature block) with every field typed as `string`. Also export a `dash(value)` helper that returns `—` for `null`, `undefined`, or empty strings and otherwise returns the value coerced to string. Verify by adding a unit test in `src/lib/pdf/__tests__/pi-data.test.ts` that asserts `dash(null) === '—'`, `dash('') === '—'`, `dash(0) === '0'`, and `dash('台灣') === '台灣'`.
- [x] 2.2 Implement the `inquiryToPIData(inquiry)` pure function in `src/lib/pdf/inquiry-to-pi-data.ts` per the "Adapter layer pattern Prisma Inquiry to PIData" decision and the "Em-dash for unstored fields" decision so the function maps the Prisma payload (with `product`, `buyer.company`, `seller.company` relations) into `PIData` using `dash` for every absent field — centralizing the Em-dash fallback for unstored fields requirement inside the adapter, never in JSX. The adapter MUST NOT import from `src/lib/db` and MUST NOT issue database calls. Verify by adding `src/lib/pdf/__tests__/inquiry-to-pi-data.test.ts` covering Adapter purity scenarios from the spec: same input produces same output (deep equality across two calls), missing `buyer.company` resolves customer-block cells to `—`, missing `product` resolves line-item description to `—`, and `quoted_price`/`quoted_quantity` flow into the single line-items row.

## 3. React-PDF component (Pro Forma Invoice layout, Component isolation, CJK glyph rendering)

- [x] 3.1 Implement the `<ProFormaInvoice data={...} />` component in `src/lib/pdf/pro-forma-invoice.tsx` so it renders the Pro Forma Invoice layout sections — seller block, customer block, shipping details box, line items table, totals breakdown, additional details block, signature block — each enclosed in a visible boxed `View` with `borderWidth`, producing a single A4 (595×842 pt) Page. The component MUST receive only `PIData` and MUST NOT import from `@prisma/client` or `src/lib/db` (the Component isolation from Prisma requirement). Verify by `grep -E "from '@prisma/client'|from '@/lib/db'" src/lib/pdf/pro-forma-invoice.tsx` returning no matches, and by visual inspection of a rendered fixture confirming all seven boxed sections appear.
- [x] 3.2 Apply the bundled Noto Sans CJK font as the default font family in the component's `StyleSheet` so the CJK glyph rendering requirement holds — Traditional Chinese, Simplified Chinese, Japanese, and Korean characters render as glyphs (not empty boxes). Verify by adding a fixture in `src/lib/pdf/__tests__/render.test.ts` containing `台灣愛文芒果`, `日本和牛 A5`, `Tokyo Trading Co., Ltd.` and asserting the resulting PDF `Buffer` includes a font reference for the Noto Sans CJK family (search the buffer for the substring `NotoSansCJK` — react-pdf embeds the font's PostScript name `NotoSansCJKtc-Regular`, the spaceless form, not the registered family display name `Noto Sans CJK`).
- [x] 3.3 Confirm the component renders all section boxes even when every cell value is `—` (the Pro Forma Invoice layout sections "Empty data still draws all sections" scenario). Verify by adding a renderer test that passes an all-em-dash `PIData` fixture and asserts the rendered Buffer's byte length is at least 50% of an **ASCII-only filled** fixture's byte length (proving structure draws regardless of content). The comparison fixture is ASCII-only on purpose: CJK glyph outlines in the embedded font subset dominate byte size, so comparing against a CJK-filled fixture would swamp the structural signal (measured empty/CJK-filled ≈ 0.19 vs empty/ASCII-filled ≈ 0.83).

## 4. Render orchestrator and font registration

- [x] 4.1 Implement `renderProFormaInvoice(data: PIData): Promise<Buffer>` in `src/lib/pdf/render.ts` per the "Font registration strategy" decision so `Font.register({ family: 'Noto Sans CJK', src: path.join(process.cwd(), 'src/lib/pdf/fonts/NotoSansCJK-Regular.ttf') })` runs at module top-level (not inside the function) and `renderToBuffer(<ProFormaInvoice data={data} />)` returns a Buffer. Verify by adding a renderer smoke test in `src/lib/pdf/__tests__/render.test.ts` that asserts `(await renderProFormaInvoice(fixture)).subarray(0, 5).toString() === '%PDF-'` and that the registration helper is invoked once across multiple renders (e.g., spy on `Font.register` or assert it's not called inside the function body via source grep).

## 5. Route integration (PDF response contract, Preserve route side-effects)

- [x] 5.1 Update `src/app/api/trade/inquiries/[id]/quotation.pdf/route.ts` so the GET handler calls `renderProFormaInvoice(inquiryToPIData(inquiry))` instead of `createSimplePdf(...)`, while keeping the existing `findFirst` with its `product`, `buyer.company`, `seller.company` includes, the existing auth guard, the existing `Content-Type: application/pdf` and `Content-Disposition: inline; filename="quotation-<short-id>.pdf"` headers (the PDF response contract requirement), and the existing `prisma.inquiry.update` block that sets `quotation_pdf_url` and flips `sent → replied` verbatim (the "Preserve GET side-effect behavior verbatim" decision and the Preserve route side-effects requirement). Verify by adding `src/app/api/trade/inquiries/[id]/quotation.pdf/__tests__/route.test.ts` covering: (a) seller GET returns 200 with `Content-Type: application/pdf` and body starting `%PDF-`; (b) buyer GET returns 200 with correct `Content-Disposition` filename; (c) GET on a `sent` inquiry flips status to `replied` and sets `quotation_pdf_url`; (d) GET on a `replied` inquiry leaves status unchanged but still sets `quotation_pdf_url`; (e) GET by an unrelated user returns `RESOURCE_NOT_FOUND`.

## 6. Manual verification with seeded data

- [x] 6.1 Run the local dev server and download PDFs for two seeded inquiries containing CJK content from `scripts/local/seed-quotations.ts` (one `replied` from the seller-side seed and one `negotiating` from the buyer-side seed) to confirm the CJK glyph rendering and Pro Forma Invoice layout requirements hold against real fixture data. Verify by saving the two downloaded PDFs and confirming by visual inspection that `台灣愛文芒果 (Mango Premium)`, `Tokyo Trading Co., Ltd.`, `Kobe Foods K.K.`, and `Horizon AI Trading` all appear as readable text, every section box is drawn, and unstored fields show em-dash.

## 7. Follow-up flag for dead code

- [x] 7.1 Add an explicit follow-up note in the change's parking commit message and in a top-of-file comment in `src/lib/pdf.ts` indicating that `createSimplePdf` is unreferenced after this change per the "Leave createSimplePdf as dead code" decision and is scheduled for deletion in a subsequent change. Verify by `grep "deprecated" src/lib/pdf.ts` returning a comment line that mentions the follow-up.
