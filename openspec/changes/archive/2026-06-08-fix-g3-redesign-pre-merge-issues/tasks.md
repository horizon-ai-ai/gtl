## 1. Branch setup

- [x] 1.1 Switch the working tree to the PR branch so all fixes land on `feat/g3-ui-redesign` (not main): run `git checkout feat/g3-ui-redesign` and `git pull origin feat/g3-ui-redesign`; verify with `git status -sb` showing `## feat/g3-ui-redesign...origin/feat/g3-ui-redesign` and a clean tree.

## 2. Lifecycle route hardening (review #1)

- [x] 2.1 Extend the allowed-transition table in src/lib/project-orders.ts with the trade fulfilment continuations so "Lifecycle advances validate status transitions and record history" has legal edges: `confirmed` additionally allows `shipped`, and `shipped` allows `completed` and `cancelled`. Verified by lifecycle-gates tests in task 2.4 (illegal `quoted -> shipped` rejected, legal `confirmed -> shipped` accepted) and by existing project-orders tests still passing via `npm run test`.
- [x] 2.2 Remap STAGE_TO_STATUS onto a validated trade status chain in src/app/api/orders/[id]/lifecycle/route.ts (`order_confirmed -> confirmed`, `shipped -> shipped`, `stocked_inbound -> completed`) and route every status change through `assertProjectTransition(order.status, newStatus)` plus a `writeOrderStatusHistory` row, with the order update and history write in one `prisma.$transaction`. Behavior: an admin advancing a `quoted` order to `order_confirmed` yields status `confirmed` with a `quoted -> confirmed` history row written atomically; an advance whose implied transition is illegal returns a CONFLICT error and writes nothing. Verified by the lifecycle-gates tests in task 2.4.
- [x] 2.3 Enforce that order owners cannot change status through lifecycle advances: in the same route, owner lifecycle advances are metadata-only — non-admin callers may target only `processing`, `in_transit`, or `arrived_warehouse` (one step forward, status untouched); targeting `order_confirmed`, `shipped`, or `stocked_inbound` as non-admin returns BUSINESS_RULE_VIOLATION with a message pointing to the quote-acceptance/admin flows. Verified by the lifecycle-gates tests in task 2.4.
- [x] 2.4 Add src/app/api/orders/[id]/lifecycle/lifecycle-gates.test.ts (jest, mocked prisma/auth following the pattern of src/app/api/orders/[id]/downstream-gates.test.ts) covering: owner targeting a status-changing stage → BUSINESS_RULE_VIOLATION and no `prisma.order.update` call; owner advancing `processing` one step → metadata updated, status argument unchanged; admin advancing `order_confirmed` on a `quoted` order → status `confirmed` and one OrderStatusHistory write inside a transaction; admin advance with an illegal implied transition (e.g. `quoted` order targeting `shipped`) → CONFLICT and no write. Verified by `npm run test` passing.

## 3. Order list date filters (reviews #5, #6)

- [x] 3.1 Ensure date filter parameters are validated in GET src/app/api/orders/route.ts: parse each of `date_start`, `date_end`, `quote_date_start`, `quote_date_end` once; if `Number.isNaN(parsed.getTime())`, throw `ApiError("VALIDATION_ERROR", ...)` so the response is HTTP 400 and no Prisma query runs. Behavior: `/api/orders?date_start=not-a-date` returns 400 instead of 500. Verified by a route unit test (or, if the route lacks a test harness, by manual curl against local dev returning the 400 envelope).
- [x] 3.2 Make end-date filters include the entire selected day: validate date filter params and use an exclusive next-day upper bound — `date_end`/`quote_date_end` filter with `lt: <parsed date + 1 day>` instead of `lte: <midnight of the parsed date>`. Behavior: an order created 2026-06-07T10:00:00Z matches `date_end=2026-06-07`; one created 2026-06-08T00:00:00Z does not. Verified by the same route test as 3.1 (boundary table from the order-list-filtering spec) or manual verification against the local DB.

## 4. UI restorations (reviews #2, #3, #4, #7) — restore regressed UI behaviors in place, no new components

- [x] 4.1 Restore regressed UI behaviors in place, no new components — starting with the draft-order 建議品項 block on the redesigned orders list: OrderCard in src/app/(app)/orders/page.tsx renders `metadata.suggested_items` (list) and `notes` summary for orders gated by `status === "draft" && (suggested_items?.length || notes)`, matching the pre-redesign block from origin/main. Verified manually in the browser: a draft order with suggested_items shows the block; non-draft orders show no change.
- [x] 4.2 Replace the hardcoded mock creator with data-driven rendering: src/components/app/trade-order-timeline.tsx accepts a `creatorName` prop and renders `成立者 {creatorName}` at the order-created node only when the prop is provided; src/app/(app)/orders/[id]/page.tsx passes `order.customer?.name` (already snapshotted at order creation by the create-order route from the inquiry buyer's CompanyProfile — no write-path change needed, works retroactively for existing trade orders). The literal `SHINKA` stays deleted; PL/CI buttons stay as-is. Behavior: a trade order whose `customer.name` is set shows that name as 成立者; orders without it show no creator line; demo orders keep their look via the demo buyer's company being named SHINKA in data, not code. Verified by `git grep SHINKA -- src` returning nothing and a manual look at a trade order detail page showing the buyer company name.
- [x] 4.3 Restore collapsed-sidebar logout in src/components/app/app-shell.tsx: move the `sidebar-expanded-only` class off the logout `<form>` and back onto the `登出` label `<span>`. Behavior: with the sidebar collapsed, the logout icon button remains visible and clickable; only the text label is hidden. Verified manually in the browser in collapsed and expanded states.
- [x] 4.4 Fix the completed-order progress connector in src/components/app/trade-order-timeline.tsx: when no stage is `active` because all stages are done (`activeIdx === -1`), render the filled connector at 100% width instead of 0%. Verified manually (or via a component assertion) with an order whose `metadata.lifecycle_stage` is `stocked_inbound`: all nodes checked and the bar full.

## 5. Verification and PR update

- [x] 5.1 Full local verification on the PR branch: `npm run lint` and `npm run test` pass (run via bash on Windows per project convention), and the production build (`npm run build` via bash) compiles with no new errors. Verified by command exit codes and clean output.
- [x] 5.2 Push the fixes to `origin/feat/g3-ui-redesign` so PR #5 updates, and leave a PR comment summarizing the 7 fixes with a link to docs/20_code_review_g3_ui_redesign.md, including the design note that `order_confirmed` now maps to status `confirmed` (not `in_execution`) for the redesign author to confirm. Verified by `gh pr view 5` showing the new head commit and the comment.
