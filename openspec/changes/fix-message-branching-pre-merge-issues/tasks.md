# Tasks — fix-message-branching-pre-merge-issues

All implementation happens on the PR branch `codex/admin-model-settings-db-only`. Commit after each numbered group (or each task where noted) with conventional titles, short bullet bodies, and a `Change: fix-message-branching-pre-merge-issues` trailer.

## 1. Append primitive and backfill

- [ ] 1.1 Implement design "Decision: single appendMessage primitive owns parent assignment and leaf bump": add `appendMessage(conversationId, data, options?)` to `src/lib/conversation/active-path.ts` that defaults the parent to the conversation's current `active_leaf_message_id`, accepts an explicit `parentMessageId` for sibling-creating flows, and creates the message plus bumps the leaf inside one `prisma.$transaction` (leaf re-read inside the transaction). Behavior: appended messages always join the active path; a transaction failure leaves message and leaf unchanged. Verify: new unit test covering parent defaulting, explicit-parent override, and leaf bump.
- [ ] 1.2 Ensure all conversation-message writers preserve the append invariant: migrate the conversations messages route, `createGenerationPlaceholder` in `src/lib/conversation/dispatch/shared.ts`, the website-builder orchestrator's message writes, the order_form write in `src/app/api/orders/route.ts`, and the user/assistant writes in `src/app/api/chat/messages/route.ts` to `appendMessage`. Behavior: an order submitted with a `conversation_id` and chat-route messages appear in subsequent branch-aware conversation reads; concurrent sends serialize on the leaf. Verify: grep shows no direct `prisma.message.create` for conversation messages outside the helper; route tests pass; manual check that an order_form message is returned by GET conversation against the local `gtl-pg` DB.
- [ ] 1.3 Implement "Legacy conversations are backfilled into the message tree" per design "Decision: migration backfills parent chains instead of read-time repair": new Prisma migration that, per conversation, chains each NULL-parent message (except the earliest) to its `created_at` predecessor and sets `active_leaf_message_id` to the latest message. Behavior: a pre-migration conversation keeps its full history visible after the first new message. Verify: seed a flat 5-message conversation on the local DB, apply the migration twice — first run chains m2..m5 and sets the leaf, second run changes zero rows (idempotent); send a new message and confirm GET returns all six.

## 2. Active-path read correctness

- [ ] 2.1 Implement "Decision: root messages are never siblings of each other" (spec requirement: Root messages are never siblings of each other): `siblingsFor` in `src/lib/conversation/active-path.ts` returns a singleton group (count 1, index 0, ids [self]) for any NULL-parent message. Behavior: legacy flat conversations show no version arrows; true siblings (shared non-NULL parent) still paginate. Verify: unit tests for both scenarios.
- [ ] 2.2 Implement "LLM context is built from the active path" per design "Decision: LLM context builders read the active path": add `loadActivePathHistory(conversationId, limit)` to `src/lib/conversation/active-path.ts` and replace the two flat `findMany` history reads (preTaskHistory and the main history) in the conversations messages route with one active-path load per request. Behavior: after an edit creates a sibling branch, the history fed to intent inference and prompt construction contains the edited version only — not the original message or its abandoned reply. Verify: unit test asserting the edited-away branch is excluded from the returned history.

## 3. Generation dispatch correctness

- [ ] 3.1 Implement "Revision prompts only accept generation results as the prior version" per design "Decision: revision source must be a generation result": the prior-version query in `sourceTextFromPriorVersion` (`src/lib/conversation/dispatch/text-generation.ts`) filters on `message_type: generation_result` in both the explicit-source and latest-fallback branches; with no qualifying result the dispatcher builds the creation prompt (no 被修改的前版內容 block, no revision instructions). Verify: unit tests — user-message source id rejected (creation prompt built), generation-result source accepted (revision prompt built), no results at all (creation prompt built).
- [ ] 3.2 Implement "Message edits only trigger paid regeneration within the generation's lineage" per design "Decision: edit-triggered regeneration requires lineage membership": in `handleEditedMessage` (`src/app/(app)/generate/page.tsx`), set `shouldRegenerate` only when the edited message is the target generation's recorded instruction message or an ancestor on its branch path (walk over already-loaded client messages). Behavior: editing an unrelated consultative message sends a plain edit with no `proceed_generate` quick reply and consumes no credits; editing the generation's instruction message still regenerates. Verify: unit test of the extracted lineage-guard decision function covering both scenarios.

## 4. Cancellation reliability

- [ ] 4.1 Implement "Stop before server acknowledgment still cancels server work" per design "Decision: pending-cancel handshake for early stop": `stopActiveMessage` in `src/hooks/useConversations.ts` records a pending cancel when the active assistant id is still a `local-` placeholder, and the hook issues the server cancel as soon as the server message id arrives (send response or `message.created` event); a failed send discards the pending cancel with the existing abort cleanup. Behavior: stopping early ends the generation as cancelled instead of completing and billing. Verify: hook unit test — pending cancel fires `cancelMessage` once the server id arrives; no cancel call when the request fails with no id.
- [ ] 4.2 Implement "Cancellation requests survive concurrent streaming writes" per design "Decision: cancel flag stored merge-safe against streaming flushes": `updateGenerationMessage` in `src/lib/conversation/dispatch/shared.ts` preserves `cancelRequested` (and related cancellation fields) recorded after the in-memory snapshot was taken, and the legacy streaming path in the conversations messages route gains the same merge-safe write plus the missing cancel check in its streaming loop. Behavior: a cancel landing between two flushes terminates the generation as cancelled on every streaming path. Verify: unit test simulating a cancel write between two metadata flushes and asserting the flag survives and the loop terminates as cancelled.

## 5. Settlement and intent-contract cleanup

- [ ] 5.1 Implement "Post-response work is scheduled through the runtime lifecycle extension" per design "Decision: marketing-intelligence settle runs under the runtime lifecycle extension (waitUntil)": wrap the marketing-intelligence settle (metadata update + ready-event publish) in the conversations messages route with `waitUntil` from `@vercel/functions` instead of a detached `void promise.then` (Next 14.2.35 ships no `after()`; see updated design decision). Behavior: intelligence results persist and the ready event fires even when the serverless instance would otherwise freeze at response return. Verify: type check passes (import resolves); unit/route test asserting the settle callback is registered through `waitUntil` (mocked) rather than executed as a detached continuation.
- [ ] 5.2 Implement design "Decision: wantsGeneration removed from the intent contract": delete the `wantsGeneration` field from the intent-resolver prompt text, parsed schema, and types in `src/lib/conversation/intent-resolver.ts`. Behavior: the refine discuss-first flow is unchanged and the refine reply still attaches its generate confirmation quick action. Verify: grep finds no `wantsGeneration` anywhere under `src/`; regression test (or existing route test extended) asserting a refine-classified turn's reply includes the generate confirmation quick action.

## 6. Verification gate

- [ ] 6.1 Full verification: `npm test`, `npx tsc --noEmit`, and lint all pass on the PR branch; manual pass against the local `gtl-pg` DB confirms the three data-visibility behaviors (legacy history intact with no version arrows, order_form message visible, branch switch never shrinks below the switched-to path). Behavior: the Implementation Contract's acceptance criteria are all satisfied. Verify: command outputs captured in the apply session; any failure blocks completion.
